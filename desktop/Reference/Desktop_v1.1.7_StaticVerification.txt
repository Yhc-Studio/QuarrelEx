QuarrelEx Desktop v1.1.7 static verification
============================================

- Project version remains 1.1.7.
- Runtime target remains 6.9.3 / QXR1 v5; no runtime/IPS changes are required for this editor-only update.
- Added QuarrelExStage v1 (*.qexstage.json) per-stage map + referenced Terrain TSA/Attr export/import.
- Stage-package import is transactional: validation/application occurs on a temporary BattleCityRom before commit.
- Added StageCanvas Select / Move mode with single-cell/rectangle selection and visual overlay.
- Added drag move, Ctrl+drag copy, Ctrl+C/X/V, Delete/Backspace and arrow-key nudge.
- Existing map paint/right-click terrain-pick workflow remains available.
- Existing ROM-wide QuarrelExConfig v3 support remains unchanged.
- Runtime 6.9.3 / QXR1 v5 EnemyCounterDisplay and Config $FF screen-byte compatibility remain present.
- C# modified files passed delimiter/event-reference static checks in the packaging environment.
- .NET SDK is not installed in the packaging environment, so the final Windows compile must be verified by Visual Studio 2022 / GitHub Actions.
